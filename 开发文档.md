# 项目开发维护指南

## 核心原则

**不动 `index.html`、`style.css`、`manifest.json` 这三个文件，就不用重新打包APK。**

---

## 一、文件说明

| 文件/目录 | 说明 |
|-----------|------|
| `index.html` | App主窗口、JS加载器。**禁止修改** |
| `style.css` | 全局样式。**禁止修改** |
| `manifest.json` | App配置。**禁止修改** |
| `js/page1.js` | 首页（本地文件，随App打包） |
| `js/page2-6.js` | 文章/音乐/视频/相册/精彩页（上传静态空间） |
| `js/1-1.js` | 更新日志页（上传静态空间） |

---

## 二、日常维护方法

### 2.1 只动 js/ 文件夹

- 修改 `js/page1.js` → 需重新打包APK（首页内容变化少，几乎不用动）
- 修改 `js/page2-6.js`、`js/1-1.js` → 上传静态空间 → App点刷新按钮即可

### 2.2 更新流程

1. 修改对应的 JS 文件
2. 上传到静态托管空间（与现有文件同名覆盖）
3. App内点击右上角刷新按钮加载新内容

### 2.3 防缓存

所有带 `?js=` 的链接已自动添加时间戳，无需手动处理。

---

## 三、JS 文件结构

```javascript
const pageData = {
  pageTitle: "页面标题",
  nav: {
    menuList: [
      { text: "首页", link: "./index.html?js=./js/page1.js", isCurrent: true },
      { text: "文章", link: "./index.html?js=外部JS路径", isCurrent: false },
      // ... 共6个菜单项
    ]
  },
  content: `<div>页面HTML内容</div>`
};
```

---

## 四、常见修改示例

### 4.1 修改页面内容
编辑对应JS文件的 `content` 部分。

### 4.2 添加外链（精彩页）
```html
<a href="https://目标网址" style="display: block; padding: 12px; background: #f5f5f5; border-radius: 8px; margin-bottom: 10px; text-decoration: none; color: #333;">
  <div style="display: flex; align-items: center; gap: 10px;">
    <div style="font-size: 24px;">📌</div>
    <div>
      <div style="font-weight: 500;">链接标题</div>
      <div style="font-size: 12px; color: #666;">链接描述</div>
    </div>
  </div>
</a>
```

### 4.3 添加更新日志记录
```html
<div style="background: #fff; border: 1px solid #eee; border-radius: 10px; padding: 15px; margin-bottom: 10px;">
  <div style="display: flex; gap: 8px; margin-bottom: 8px;">
    <span style="background: #667eea; color: white; padding: 2px 8px; border-radius: 4px; font-size: 11px;">2026-05-04</span>
    <span style="background: #e8f5e9; color: #388e3c; padding: 2px 8px; border-radius: 4px; font-size: 11px;">新增</span>
  </div>
  <div style="font-weight: 500; font-size: 15px; margin-bottom: 4px;">更新标题</div>
  <div style="font-size: 13px; color: #666;">更新说明</div>
</div>
```

---

## 五、需要重新打包的情况

- 修改 `index.html`
- 修改 `style.css`
- 修改 `manifest.json`（App名称、图标、包名等）
- 修改 `js/page1.js`（首页内容变化）

这些情况很少发生，日常只需更新 js/ 下的外部页面文件即可。
